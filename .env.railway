# Production environment variables for Railway
# These will be set in Railway dashboard UI
# DATABASE_URL=postgresql://user:password@host:port/database (auto-generated by Railway PostgreSQL plugin)
# SESSION_SECRET=your-production-secret-key-here
NODE_ENV=production
